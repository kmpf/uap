language: python

matrix:
  include:
  # Ubuntu 12.04.5 LTS
    - os: linux
      python: '2.6'
    - os: linux
      python: '2.7'
    - os: linux
      python: '3.3'
    - os: linux
      python: '3.4'
    - os: linux
      python: '3.5'

  # Ubuntu 14.04.3 LTS
    - os: linux
      dist: trusty
      python: '2.6'
    - os: linux
      dist: trusty
      python: '2.7'
    - os: linux
      dist: trusty
      python: '3.3'
    - os: linux
      dist: trusty
      python: '3.4'
    - os: linux
      dist: trusty
      python: '3.5'

install:
  - ./bootstrap.sh

script: ./uap travis_example/travis_u.yaml status > /dev/null
