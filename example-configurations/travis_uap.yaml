
# This is the SmallRNA Kit test 
# 

destination_path:  travis_output

steps:
    # read sourcefile
    read_test_file (raw_file_source):
        pattern: sources/*.txt
        group: (\w+).txt
        sample_id_prefix: Result


tools:
       
        cat:
            path: 'cat'
            get_version: '--version'

        grep:
            path: 'grep'
            get_version: '--version'

        cp:
            path: 'cp'
            get_version: '--version'

        echo:
            path: '/bin/echo'
            get_version: '--version'
